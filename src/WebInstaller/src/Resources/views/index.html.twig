{% extends "base.html.twig" %}

{% block main %}
    <div class="card__title">
        <h2>{{ 'shuwei.update.steps.start.title'|trans }}</h2>
    </div>

    <div class="card__body is--align-center">

        <div class="welcome-illustration">
            <img src="{{ app.request.baseUrl }}/assets/images/welcome.svg" alt="">
        </div>

        <div class="welcome-container">
            <h1 class="welcome-title">{{ 'shuwei.update.steps.start.welcomeTitle'|trans }}</h1>

            <p class="welcome-intro-message">{{ 'shuwei.update.steps.start.welcomeMessage'|trans }}</p>

            <label for="language">
                {{ 'shuwei.update.steps.start.languageSelection'|trans }}
                <a class="help-badge" href="#" data-tooltip="{{ 'shuwei.update.steps.start.languageSelectionInfo'|trans }}">
                    <i class="icon-help"></i>
                </a>
            </label>

            <form class="select-wrapper language" method="get">
                <img class="language-flag" src="{{ app.request.baseUrl }}/assets/images/flags/{{ app.request.locale }}.png" alt="{{ app.request.locale }}">
                <select id="language" name="language" class="language-selection" onchange="this.form.submit();">
                    <option value="zh"{% if app.request.locale == "zh" %} selected{% endif %}>
                        {{ 'shuwei.update.language.zh'|trans }}
                    </option>
                </select>
            </form>
        </div>
    </div>

    <div class="card__footer flex-container">
        <a href="{{ url('configure') }}" class="btn btn-primary flex-item flex-right">{{ 'shuwei.update.steps.start.submit'|trans }}</a>
    </div>
{% endblock %}
